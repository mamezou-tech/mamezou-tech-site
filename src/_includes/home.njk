---
description:
layout: base.njk
---

<style>
  .logo {
    margin: 0 auto 1rem;
  }
  .tdbc-hero {
    padding-top: 5vh;
    padding-bottom: 0;
  }
  .article-area {
    width: 100%;
  }
  .additional-area {
    border: #bebaba solid 1px;
    border-radius: 8px;
    padding: 1rem;
    margin-top: 3rem;
    box-shadow: 5px 5px 2px #4f4f4f;
    background-image: url('/img/logo/mameka_280.png');
    background-size: auto;
    background-repeat: no-repeat;
    background-position: right 1rem top 4rem;
    background-blend-mode: overlay;
    background-color: rgba(255, 255, 255, 0.5);
  }
  .additional-area h2 {
    border-bottom: #4f4f4f solid 1px;
    padding-bottom: 0.7rem;
    font-size: 1.4rem
  }
  @media (min-width: 80ch) and (max-width: 768px) {
    .article-area {
      width: 50%;
      float: left;
    }
    .additional-area {
      width: 50%;
      float: right;
    }
  }
  @media (min-width: 769px) {
    .article-area {
      width: 60%;
      float: left;
    }
    .additional-area {
      width: 40%;
      float: right;
    }
  }
  .post-list {
    padding-inline-start: 0;
  }
  .article-area {
    padding: 0 2rem;
  }
  a.all-article-link {
    font-size: 0.85rem;
    margin-left: 1rem;
    text-decoration: none;
  }
  .home-social-plugins {
    margin: auto;
  }
  .tags {
    border: solid 1px;
    margin: 1.5rem 0.5rem 0 0.5rem;
    padding: 0.5rem;
    border-radius: 8px;
    border-color: #624176;
  }
  .tags a {
    margin: 0.5rem;
    line-height: 1.5rem;
  }
  .icon {
    margin: 0 0.25rem;
    display: inline-flex;
    vertical-align: bottom;
  }
</style>
<header class="tdbc-hero">
  <div class="tdbc-container">
    <div class="logo">
      <img src="/img/logo/mz-tech-logo-small.png" alt="mz-logo" width="100%" height="100%" />
    </div>
    <div>
    <h1 class="tdbc-ink--mz tdbc-mb-none tdbc-top-title">{{ meta.siteName }}</h1>
    <h2 class="tdbc-lead tdbc-content-maxlength tdbc-ink--gray tdbc-mt-md tdbc-mx-auto">
      {{ meta.siteDescription }}
    </h2>
    </div>

    <div class="home-social-plugins">
      <div id="preact-sns"></div>
      <script type="module">
        import { render } from '/components/preact/social_plugins.js';
        render({url: '{{ meta.url }}', path: '{{ page.data.url }}'}, document.getElementById("preact-sns"));
      </script>
    </div>
    {%- include "google-search.njk" %}
  </div>
</header>
<main class="tdbc-container">
  <div class="tdbc-section">
    <ul class="tdbc-column-container">
      {%- for page in search.pages('pages exclude!=true') -%}
      <li class="tdbc-card">
        <div class="tdbc-card__content">
          <a href="{{ page.data.url }}" class="tdbc-card__title">{{ page.data.title }}
            {%- if page.data.icon -%}<img alt="icon" class="icon" width="28px" src="{{ page.data.icon }}" />{%- endif -%}</a>
          <p>{{ page.data.description }}</p>
        </div>
      </li>
      {%- endfor -%}
    </ul>
  </div>

{{ content | safe }}
  <div>
    <fieldset class="tags">
      <legend>記事のタグから検索</legend>
      {%- include "tags-links.njk" %}
    </fieldset>
  </div>
  <p style="font-size:1rem;margin:1rem;">本サイトは本家の豆蔵ホームページではありません。会社概要、製品紹介等は<a class="new-tab-link" target="_blank" rel="noreferrer noopener" style="text-decoration:underline" href="https://www.mamezou.com/">こちら</a>、
  募集中の求人情報は<a class="new-tab-link" target="_blank" rel="noreferrer noopener" style="text-decoration:underline" href="https://hrmos.co/pages/mamezou-recruit/jobs">こちら</a>をご覧ください。</p>

  <div>
    <fieldset class="chatgpt-jargon">
      <legend><img alt="mameka" width="30px" height="100%" src="/img/logo/mameka6_30.png" />豆香の豆知識(Powered by GPT-4)</legend>
      <div>今日の言葉: {{ gpt.columns[0].title }}</div>
      <div>{{ gpt.columns[0].text | safe }}</div>
      <div><b>※本コラムはOpenAIのChat Completion APIで生成したものです。存在しない言葉(AIの幻覚/作話)だったり、内容に誤りがある可能性があります。</b></div>
      <div>過去のコラムは<a href="/gpt/mameka/" style="text-decoration:underline">こちら</a>から見れます!</div>
    </fieldset>
  </div>
  <div>
    <div class="article-area">
      <article class="tdbc-mx-auto" style="margin-top: 3em">
        <section class="post-list__wrapper recent-article">
          <h2 class="post-list__heading">新着記事<img alt="new-articles" class="icon" width="36px" height="100%" src="/img/icons/new-box.svg" /><a class="all-article-link" href="/articles/">全ての記事を見る</a></h2>
          <ul class="post-list">
            {% for post in search.pages('layout^=post exclude!=true', 'date=desc') | limit(10) | data %}
              <li class="post-list__item">
                <div>
                  <div class="post-list__meta">
                    <time datetime="{{ post.date | htmlDateString }}">
                      {{ post.date | readableDate }}
                    </time>
                    {%- if post.category -%}
                    <span> | </span>
                    <span class="Label">{{ post.category }}</span>
                    {% endif %}
                    <span> | </span>
                    <span>{{ post | readingTime }} read</span>

                  </div>

                  <div class="post-list__tags">
                    {% for tag in post.tags %}
                      {%- if (search.tags() | validTags).indexOf(tag) != -1 -%}
                        {% set tagUrl %}/tags/{{ tag }}/{% endset %}
                        <a href="{{ tagUrl | url }}">#{{ tag }}</a>
                      {%- endif -%}
                    {% endfor %}
                  </div>
                </div>

                <h3 class="post-list__title">
                  <a  href="{{ post.url }}">{{ post.title }}</a>
                </h3>

                <p class="post-list__excerpt">{{ post.children | excerpt | safe  }}</p>

                <a class="post-list__read-more" href="{{ post.url }}">記事を読む</a>
              </li>
            {% endfor %}
          </ul>
        </section>
      </article>
    </div>
    <div class="additional-area">
      <article class="tdbc-mx-auto">
      <h2><img alt="new-articles" class="icon" width="24px" src="https://api.iconify.design/fluent/star-emphasis-20-filled.svg?color=red" />先週の人気記事<img alt="new-articles" class="icon" width="24px" src="https://api.iconify.design/fluent/star-emphasis-20-filled.svg?color=red" /></h2>
      <ol>
        {%- for article in pv.ranking -%}
        {%- set published = search.pages('exclude!=true') | articleDate(article.url) -%}
        <li><a href="{{ article.url }}">{{ article.title }}</a></li>
        {%- endfor -%}
      </ol>
      </article>
    </div>
  </div>
  <div style="margin: 1rem 0 0 0; padding: 0;">
  <hr class="link-separator" />
    {%- include "links.njk" %}
  </div>
</main>
