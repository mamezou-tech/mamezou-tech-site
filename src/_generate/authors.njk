---
pagination:
  data: collections.contributorArticles
  size: 1
  alias: contributor
  addAllPagesToCollections: true
layout: page.njk
permalink: /{{ page.fileSlug }}/{{ contributor.name }}/{% if contributor.pageIndex > 0 %}{{ contributor.pageIndex }}/{% endif %}
eleventyComputed:
  title: "{{ contributor.name }} の記事"
---

{% set pages = pagination.pages | byAuthor(contributor.name) %}
{% set hrefs = pagination.hrefs | selectAuthor(contributor.name) %}
{% include "pagination.njk" %}

{% set postslist = contributor.articles %}
{% include "posts-list.njk" %}

{% include "pagination.njk" %}
